/**
 * Validation Scenario: Violation (Anti-Pattern).
 *
 * Classes in this package demonstrate the DRY violation: email validation logic
 * is duplicated in multiple DTOs, each with its own inline validation.
 *
 * See {@code com.orvigas.dry.validation.solution} for the refactored approach.
 */
package com.orvigas.dry.validation.violation;
